<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link rel="stylesheet" href="css/compiled.css">
</head>
<body class="h-screen">
    <header class="bg-white p-3 px-10 shadow hidden sm:block">
        <nav class="w-full flex justify-between">
            <img class="w-40" src="images/h-logo.svg" alt="knitly logo">
            <ul class="w-2/5 lg:w-1/4 flex items-center justify-between">
                <li class="cursor-pointer hover:text-indigo-400"><a href="/login">Log In</a></li>
                <li class="cursor-pointer hover:text-indigo-400"><a href="/signup">Sign Up</a></li>
                <li class="cursor-pointer hover:text-indigo-400"><a href="/">About</a></li>
            </ul>
        </nav>
    </header>
    <div class="flex justify-center content-center">
        <section class="flex justify-center">
            <section class="mt-20 flex flex-col border-2 p-5 rounded-xl bg-white bg-opacity-75">
                <h1 class="py-5">Log in to knitly</h1>
                <form action="/login" method="POST">
                    <div class="mb-5">
                    <% if (locals.messages.errors) { %>
                        <% messages.errors.forEach( el=> { %>
                            <span class="block text-red-400">
                                * <%= el.msg %>
                            </span>
                            <% }) %>
                    <% } %>   
                    </div>

                    <div>
                        <label for="email" class="block mb-2 text-sm text-gray-600 dark:text-gray-400 float-left">Email Address</label>
                        <input type="text" name="email" id="email" class="w-full px-3 py-2 placeholder-gray-300 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-100 focus:border-indigo-300 dark:bg-gray-700 dark:text-white dark:placeholder-gray-500 dark:border-gray-600 dark:focus:ring-gray-900 dark:focus:border-gray-500">
                    </div>
                    <div>
                        <label for="password" class="block mb-2 text-sm text-gray-600 dark:text-gray-400 float-left">Password</label>
                        <input type="text" name="password" id="password" class="w-full px-3 py-2 placeholder-gray-300 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-100 focus:border-indigo-300 dark:bg-gray-700 dark:text-white dark:placeholder-gray-500 dark:border-gray-600 dark:focus:ring-gray-900 dark:focus:border-gray-500">
                    </div>
                    <button type="submit" class="w-full mt-8 px-3 py-4 text-white bg-indigo-500 hover:bg-indigo-700 rounded-md focus:bg-indigo-600 focus:outline-none">Submit</button>
                </form>
            </section>

        </section> 
    </div>
    <script src="server.js"></script>
</body>
</html>